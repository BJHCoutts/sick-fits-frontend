import styled from 'styled-components';

const SCloseButton = styled.button`
  background: black;
  color: white;
  font-size: 3rem;
  border: 0;
  position: absolute;
  z-index: 2;
  right: 20px;
  cursor: pointer;
`;

export default SCloseButton;
